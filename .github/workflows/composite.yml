name: Test composite
on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Run composite
        uses: ./.github/actions/hell-composite
        with: 
          input_message: "cruel world"
        env:
          INPUT_ENV: "nice world"
