name: Checkout workflow

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Checking git log
        run: |
          echo git log output
          git log
          echo git show HEAD output
          git show HEAD
