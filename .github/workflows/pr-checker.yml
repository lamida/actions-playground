name: PR checker

on: 
  pull_request:

jobs:
  job-one:
    runs-on: ubuntu-latest
    steps: 
        - run: echo "fine 1"

  job-two:
    runs-on: ubuntu-latest
    steps: 
        - run: echo "fine 2"

  job-three:
    if: false
    runs-on: ubuntu-latest
    steps: 
        - run: echo "fine 32"

  pr-checker:
    runs-on: ubuntu-latest
    needs: 
        - job-one
        - job-two
        - job-three
    name: PR Checker
    steps:
        - run: if ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'skipped') }} ; then exit 1; fi